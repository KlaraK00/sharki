class Water extends DrawableObject {

    constructor() {
        super();
        // BACKGROUND_WATER_DARK.forEach(water => {
        //     this.loadImage(water);
        // });
        this.loadImage(BACKGROUND_WATER_DARK[0]);
    }
}